import styled from 'styled-components';
import MainContainer from '../../layout/MainContainer';

export const ChatPageMainContainer = styled(MainContainer)`
  position: relative;
  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 500;
  font-size: 20px;
  line-height: 24px;

  letter-spacing: 0.15px;

  color: #212121;
  text-align: center;

  h1 {
    margin: 16px 0;
  }
  p {
    font-weight: 400;
    font-size: 14px;

    color: #9e9e9e;
  }
`;
