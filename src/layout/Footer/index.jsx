import styled from 'styled-components';

const Footer = styled.footer`
  position: absolute;
  width: 72px;
  height: 2px;
  left: calc(50% - 72px / 2);
  bottom: 8px;

  background: #1f1f1f;
  border-radius: 8px;
`;

export default Footer;
